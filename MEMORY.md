# MEMORY.md — 进销存原型 项目记忆

> 业务背景与已确认口径。改动业务逻辑前先读本文件；与本文件冲突时先在会话中确认，不要直接改。只记长期信息，不记凭据、不记临时状态。

## 业务背景
进销存 SaaS 的「轻量发货」链路：供应商把货发到总部仓 / 门店，再由总部仓或门店发给消费者；本期覆盖发货、收货、配送差异（补发）、退货返厂、售后。
四端角色：租户后台（平台 / 总部运营）、供应商后台（供货方）、门店 APP（收货与返厂）、买家端（消费者）。

## 已确认规则（改动前必读）
1. **发货模式两档**：供应商直配 / 总部仓直配；货源 供应商供货 / 总部自有，派生标签「总部仓直配 · 自有货」。已废弃词：「总部仓发货」「总部自营」；「前置仓配送」不再作为发货模式，仅在商品表单里置灰保留、选项旁标「（暂未开放）」，不再写它的说明文案。
2. **供货单口径**：供货单只承载到内部主体（总仓 / 门店）为止的链路；「总部仓 → 消费者」属于销售订单发货，操作在订单管理。
3. **收货状态词**：数据层 status 保持 已发货 / 部分收货 / 已收货 / 收货异常；收货方视图显示「待收货」，发货方视图显示「已发货」。
4. **部分收货不单列 Tab**：归在 待收货 / 已发货 下并加「部分收货」标记（已收 X｜待补 Y）；确认收货那一刻才判定是否少收。
4.1 **收货管理页签为 全部 / 待发货 / 待收货 / 已收货 / 收货异常**（2026-09-23 补「待发货」）：待发货＝上游还没发出的单，本页只读可见；**「待收货」行的操作是「关联订单」**（点开看对应的销售订单：收件人 / 商品 / 订单数量），不放「详情」——收货现场要看的是「这单给谁」。
5. **收货判定基准 = 已发数量（sent）**，不是应发数量。
6. **超时自动确认**：只认物流「已签收」时间 +3 天；在途单（无签收时间）不进入超时池。
7. **补发三态**：待补发 → 补发中 → 补发完成；发货收口在配送差异页。
8. **审核驳回必须填不通过原因**，随单记录展示（租户 + 供应商两端）。
9. **一件代发拆成「发货归供应商、售后总部管钱」两条线**（2026-09-23 口径更新，此前为「售后全归供应商、租户只读」）：
    - **发货归供应商**：订单在租户「订单管理」只提供「详情」，不提供发货 / 备注 / 改地址等操作；供应商在「一件代发」发货。两侧共享同一份数据（orderStore）。
    - **售后＝总部管钱、供应商管货**：总部在租户「售后管理」做**审核**（同意 / 拒绝）与**退款**（确认退款）；供应商在「售后处理」只做**签收验收 / 拒签**，不参与钱款（金额只读）。
    - **仅退款**不涉及货，供应商侧不展示。**拒签不直接关单**，转「退货异常」由总部裁决（认可拒签并关单 / 仍向买家退款）。
    - **退货寄回地址**：总部在「售后管理」同意退货时从「设置 › 地址库 › 售后地址」里**选一个**发给买家；地址只在地址库这一处维护，使用方（供应商发货、总部同意退货）都只做选择。
    - 状态机：待总部审核 → 待买家退货 → 待供应商签收 → 待总部退款 → 售后完成；退货异常 / 售后关闭为分支终态。两侧共用 afterSaleStore，任一端操作另一端实时同步。
10. **供应商管理操作列静态展示**；「开通收款」与业务无关，仅作两种状态展示。
11. **门店 APP 收货 Tab 以后台为准**（2026-09-23 同步为 全部 / 待发货 / 待收货 / 已收货 / 收货异常）；每个 Tab / 筛选组合下都要有种子数据。
    门店APP 收货卡：**每张卡第一个操作都是「关联订单」**（底部弹层：销售订单号 / 下单人 / 联系电话 / 配送方式 / 商品×数量）——自提单货到要通知客户来取，店员在卡片上就该看到「这单给谁」，不必先进详情。**待收货只给「查看物流 + 确认收货」**——「确认收货」进的就是供货单详情，不再挂重复的「查看详情」；其它状态保留「查看详情」。
    门店APP 供货单字段**与后台收货管理同口径**（2026-09-23 完善，原只有 商品/数量/单号 三行）：卡片与详情都带 **规格 / 发货主体 / 发货时间 / 物流单号**（待发货无后两项，显示「未发货」）。发货主体取发货方：供应商直配＝供应商00X，总部仓直配（含自有货）＝九天教育总仓。
    **「部分收货」与「收货异常」的数量口径不能共用一句话**：部分收货是「还差着补」→ 已收 X｜待补 Y 件；收货异常是「已经少了」→ 实收 X｜差 Y 件。
    门店APP 供货单详情：**不再展示「门店信息」**（收货方＝登录门店自己，列自己的名称电话地址零信息量，且原实现是写死常量，换门店会误导）；改为常驻「**商品信息**」（名称 + 规格 + 发货数量 + 待收货时的本次应收）与「**关联订单**」（销售订单号 / 下单人 / 联系电话 / 配送方式＝上门自提）——自提单货到要通知客户来取，店员得知道这单给谁。
12.0 **「代发」标记**（2026-09-24 补订单管理）：判定口径 = **供应商直配 + 快递发货**，只写在 `data.js` 的 `isDropship()` 一处，订单管理列表直接调它，标在**订单类型列**（显示「代发」tag）。租户售后管理列表的「代发」标在**售后方式列**、按「行在不在 `afterSaleStore` 里」判定——那个 store 装的就是代发售后单（与供应商侧同一份），页面本地 `ROWS` 是自营售后。**不要再给售后种子另加 dropship 字段**（会被页面覆盖成死数据，2026-09-24 试过已撤）。供应商后台的两张列表暂不加此标。

12. **一件代发页（供应商后台）= 供应商视角的订单列表**：形态照租户「订单管理」逐项一致（页签 全部/待付款/待发货/已发货/售后中/已完成/已关闭），数据源为「供应商直配 + 快递发货」的销售订单；「门店 / 配送方式」「关联供货单」两列与「分配门店」操作**不复制**；发货回写订单并同步供货任务（同一个物理单据、两端一致）；未发货退款的代发任务状态为「已取消」；待付款单不生成供货任务。保留待付款/已关闭页签是为后续「供应商收款」形态预留。
13. **物流信息可改窗口**（填错可改，收货/签收即锁）：
    - 代发订单：已发货且未签收可改；买家签收后 / 售后中 / 已完成 / 已关闭锁定
    - 供货单（发总部仓 / 发门店）：已发货且收货方未收货可改；部分收货 / 已收货 / 收货异常锁定
    - 只改快递公司 + 单号，留痕（物流轨迹显示修改时间与原单号），改完租户侧发货管理同步显示新单号；发货数量与时间不可回改。
14. **两套地址库，各自维护、使用现场只「选」**（2026-09-23 口径更新）：
    - **租户后台「设置 › 地址库」**：租户自己的发货 / 售后 / 仓库地址；总部同意退货时从这里**选**售后地址发给买家。
    - **供应商后台「地址库」**：供应商自己的发货 / 售后 / 仓库地址（`addrStore`），是供应商侧唯一维护地址的地方。
    - 发货弹窗里**只做选择**，不再有「+ 添加地址」入口（已删）。
    - 原型简化：只有一个供应商视角，两份地址库合成演示；正式版按主体拆开。
15. **批量回传三件套**（一件代发页，面向全部待发货单、不做勾选前置）：批量发货（在线勾选 + 统一快递公司 + 逐单运单号）/ 导入发货（下载模板 → 线下填 → 导入 → 确认 → 成功，四步）/ 下载发货模板。**代发模板是订单维度**（订单号/收件人/收件人电话/收货地址/商品/规格/待发数量 为锁定列 + 快递公司/物流单号 待填），与发总部仓·发门店的**供货单维度**模板区分开。批量回传与逐单发货同口径：逐单同步订单与供货任务，发货地址取默认发货地址。
16. **锁单不做**（2026-09-22 结论）：老定制系统的「锁单」是为「Excel 往返周期」打的补丁——供应商导出→发货方填→回传→商家手工录入，往返一两天，期间用户退款会造成白发货。本期 SaaS 把发货做成系统内实时动作（逐单 + 批量 + 导入），前提不成立。若客户坚持，退化为一条规则「已发货订单不能自助退款，走商家审核」，不新增状态与页面。
17. **退款限制做在「设置 › 通用设置 › 交易设置」**（真实环境路由 `#/tenantSetting/tenantGeneralSetting`）。**「极速退款」与「限制用户退款申请」是两项，不是改名**（elton 2026-09-23 明确，此前原型误把前者替换成后者）：
    - **极速退款 = 原系统既有能力，原样保留**：开关（现网九天教育租户固定开启，说明写明「该功能必须保持开启状态」）+ 灰底条件面板（支付有礼校验 / 寄送商品 / 核销商品）+ 四条说明（用户下单说明1、说明2、商品下架、订单发货）。语义：命中条件 → 无需审核、自动通过。
    - **限制用户退款申请 = 本期新增项**（原系统没有）：开启后买家在条件下**不允许发起**退款申请；关闭则一律允许。条件沿用原系统语法（用户下单 / 商品下架 / 订单发货 / 无条件），默认「订单发货」。
    - **「关联发货单」条件不引入**（elton 2026-09-22 明确）：代发单支付即生成供货任务，按它判定等于支付后所有退款都被限制，与「待发货时自动通过」冲突。
    - 进销存两处**执行差异**：① 极速退款条件内自动通过时须同步关闭对应供货任务；② 代发单的退款审核由**总部**执行（租户后台 · 售后管理），供应商侧只做签收验收。

18. **设置里另有「地址库」「快递模板」两个复刻页**（2026-09-23 新增，照真实 SaaS 复刻）：
    - **地址库**（`#/tenantSetting/addressSetting`）：三页签 发货地址 / 售后地址 / 仓库地址，列 联系人 / 联系电话 / 所在地区 / 详细地址 / 是否默认 / 操作（编辑·设置默认·删除）；新增弹窗含 省市区街道四级联动 + 邮编。**地址只在这一处维护，使用方只做「选」**——供应商发货选发货地址、总部同意退货选售后地址。
    - **快递模板**（`#/tenantSetting/expressTemplate`）：列 模板名称 / 可配送范围 / 计费方式 / 首件(个)·首重(kg) / 运费 / 续件·续重 / 续费 / 操作（编辑·禁用·删除）；计费方式决定表头是「首件/续件」还是「首重/续重」。**与进销存无耦合**：进销存发货单里的快递公司由发货方在发货弹窗里选，不从模板取；模板只管「向买家收多少运费」。

19. **门店APP 配送差异 / 举证 / 返厂要对得上后台口径**（2026-09-23 完善，此前是一套写死的演示卡，与后台差异单对不上账）：
    - **配送差异读 diffStore 的「门店上报」部分**（`diffInTab` 复用后台的分页签判定），不再是门店端一套、后台一套演示数据；门店提交举证即写回该单，后台即时可见。举证提交必须**按本单 id 写回**，不能取「列表里任意一张待举证单」。
    - 卡片与详情按后台 10 列口径：差异单号 / 来源链路 / 上报方 / 关联供货单 / **发货方（补发责任方）** / 差异摘要 / 举证信息 / 状态 / 补发供货单 / 驳回原因。**原来的「商品数 / 配货差异总数量」删掉**——后台差异单没有这两个字段。
    - **「来源供货单」块**：发货方 / 商品 / 快递公司 / 物流单号 / 物流轨迹，从 supplyStore ∪ supplierStore 按 supplyNo 取真单据。门店手上是货和快递箱，举证时要能对上是哪一单。
    - **差异明细的实发不能用「应发 − 差异」反推**：错货时实收可能等于应收（「华为手机 应收1/实收1 错货 1 件」），反推会得出错误的实发 0。应发/实发从差异摘要 `应收N/实收M` 里取。
    - 术语「**抽查指令（破）**」是老定制系统的叫法，已删（举证页与差异单详情两处），该块改叫「差异明细」。
    - **退货返厂单不带钱**（2026-09-23 elton 明确：返厂详情里的「退款说明」不需要）：返厂单只承载货的流向，退款归「售后管理」，本单不展示退款金额 / 退款状态 / 退款说明。门店APP 列表与详情、租户后台返厂详情三处都已去掉；**供应商后台返厂详情保留一条「关于退款」的脱敏口径说明**——供应商本来就不该看到钱，这条是解释它为什么看不到。原句「门店收到退货即触发总部退款」是错的（与「退款独立执行」矛盾），一并删掉。
    - **返厂退回地址**取自供应商「地址库 › 售后地址」（`SUP_ADDRESSES` + `afterAddrOf`），发起返厂 / 返厂详情 / 填写寄出物流三处都给全（收件人 / 电话 / 地址 / 邮编）——门店要真的寄快递，只给「退回方」一个名字寄不出去。`SUP_ADDRESSES` 增加 `owner` 字段标明归属主体，供应商地址库页按 `SUPPLIER_SELF` 过滤（原型只有一个供应商登录态，正式版按主体拆开）。

20. **发货 / 收货改成「按商品汇总到门店」的批次模型**（2026-09-24 elton 定案，此前是一笔销售订单发一次）：
    - **一张供货单 = 一批货**，覆盖多笔销售订单、多种商品。数据形态 `{ id, leg, orderNos[], items[{ product, spec, emoji, qty, sent, received, from:[{ orderNo, qty }] }], packages[], qty, sent, received, status }`。`from` 是「这件商品来自哪几笔订单、各几件」，全站靠它反查订单。
    - **入口是订单池，不是表单**（2026-09-24 elton 纠正：表单页保持原样，不要自动聚合列表）。发货管理分两个页签：**自提订单** + **发货任务**（页签名 2026-09-24 定案：叫过「待发货订单」「待汇总订单」都被否——前者跟任务行的「待发货」状态撞名，后者太抽象；「自提订单」直接说清池子里是什么）。池里点「生成发货任务」→ 选要发的门店（可多选）+ 选**支付时间截止**（如「截止到今天 17:00 之前支付的订单」）→ 按门店各生成一张任务，状态**待发货**；截止时间之后支付的留在池里等下一批。生成是**显式动作**，不自动截批。
    - **生成时不是发货**（2026-09-24 elton 纠正两次）：生成只产出待发货任务，物流单号在发货时才填。**任务生成即锁死**——2026-09-24 elton 明确删掉操作栏的「追加订单」（原设计是「未发货可追加」，已撤），同一批货不再改来改去。
    - **批量发货 / 导入发货 / 下载发货模板**从表单页移到发货任务页。**三个都保留**（2026-09-24 elton 定：门店多、或快递单号习惯线下整批填的租户需要导入）。批次维度下的口径：**一行 = 一张发货任务（发往一个门店）**，锁定列是 供货单号 / 发货主体 / 收货主体 / 商品 / 未发数量，只填快递公司 + 物流单号；**一行只登记一个包裹**，这批要拆多包裹的必须单张点「发货」逐个登记。
      - 老口径「同一批必须是同一发货方 + 同一收货人 + 同一收货地址」**已作废**——那是订单维度时代防止串批的规则，批次模型下每行本来就是不同收货人，写它自相矛盾。
      - 供应商后台「一件代发」页的那三个**保持订单维度不动**（模板一行一笔代发单），别跟发货任务页的批次模板混。
    - **多包裹**：一批装不下就拆包，一个包裹一条运单号，**上限 50 个**（到顶后「添加包裹」置灰）。
    - **物流轨迹按包裹分页签**（2026-09-24 elton 要求）：一批几个包裹几个页签，每个包裹一条运单 + 一条独立轨迹。**页签条最多占 5 个位置**——超过时前 4 个平铺、其余收进「更多 N 个包裹」下拉；选中第 5 个及以后时**下拉本身变成「包裹 7 ▾」页签并高亮**，保证当前包裹一定看得见。种子里 `FHD2609180093` 特意铺了 7 个包裹当样本。
    - **物流轨迹的取数必须走 `packagesOf(doc)`**：原来读的是 `doc.carrier / doc.tracking`（订单维度的字段），批次单据上不存在，页面上显示成空白 + `undefined`。表格里显示「共 N 个包 / 查看 N 个包裹」；**只在「添加包裹」这一处**，不要另造「拆包」这类第二个入口或第二个名称（elton 2026-09-24 指出重复）。
    - `batchNo` 防止同一笔订单重复进池：订单一旦进了批次就不再出现在订单池。
    - 供应商后台同此模型（发门店 / 发总仓两页同样分「待发货订单 + 发货任务」）。新单据按 leg 路由写库：`supplier_to_hq` / `supplier_inbound` 双写 supplyStore + supplierStore，`sup_consumer` 只写 supplierStore，其余只写 supplyStore。
21. **发货弹窗的地址只从地址库来，默认地址唯一**（2026-09-24 elton 明确）：租户取「设置 › 地址库 › 发货地址」，供应商取「地址库 › 发货地址」（按 `owner` 过滤）；弹窗打开即选中 `isDefault` 那条，**不再两张都标「默认」**（原来地址是写死常量，且两条都挂了默认标）。发货弹窗严禁写死地址。
22. **商品明细带「订单关联」列**（2026-09-24 elton 要求「这个商品到底多少用户下单了，要能看到」）：发货弹窗商品行显示「N 笔订单」，点开 `OrderRefsPop` 给订单号 / 购买数量 / 购买者 / 联系电话 / 配送方式 / 下单时间；自提单靠它知道货到了通知谁。**供应商侧同列但买家昵称与电话脱敏**（供应商只管把货发到门店，不该拿到消费者联系方式），弹层里写明这一点。
23. **供应商地址库只有「发货地址 / 售后地址」两个页签**，没有「仓库地址」（2026-09-24 elton 明确：供应商不持有仓）。租户地址库仍是三个页签。实现上是 `AddressBook` 的 `tabs` 参数，供应商传 `SUP_ADDR_TABS`。
24. **供货单只为「上门自提」的订单服务**（2026-09-24 elton 指出后修正）：快递单由发货方直发消费者，货不经过总仓 / 门店，**不进任何供货单、不进订单池**。三处都按这条口径：`POOL_SCOPES.sup_hq` 补上「上门自提」条件（原来漏了，快递单就是从这漏进去的）；种子里内部供货单的 `from` 全部换成真实自提单；自提单**没有「收件人 / 收件人地址」字段**（原来是错的——自提单的收件人地址被写成门店名），只有 昵称 + 收件人电话。
    - **订单池按链路判重，不是一票否决**：同一个订单先走「供应商 → 总仓」再走「总仓 → 门店」，两条链路各算一次入池，所以订单上记的是 `batchNos` 数组、`poolOf` 只排除**同链路**已入批的。
    - **自提订单页的列**（2026-09-24 elton 要求去掉冗余）：商品信息 / 数量 / 买家（昵称 + 电话两行）/ 自提门店 / 支付时间 / 供货模式。**没有「配送方式」列**——池子里恒为上门自提，不占一列；**门店列叫「自提门店」**，自提单里它不是发货目的地，是买家去取货的地方。买家列**只给昵称 + 电话**，不要把 `buyer` 对象整个摊开（会把收件人、地址一起印出来，自提单里那就是噪音）。
25. **「库存回写（本期预留）」区块已删**（2026-09-24 elton 明确）：供货单详情里那段「本期只做销、不落可售库存、留作第二版回写凭证」的展示连同收货时写的 `stockWriteback` 字段一起去掉。**但需求注释里保留一行口径「本期实收不落到可售库存」**——那是给评审看的设计边界，不是给租户看的。
26. **收货管理的「关联订单」抽屉按多笔订单改**（2026-09-24）：一张供货单覆盖多笔订单，抽屉要逐笔列出（订单号 / 购买者 / 电话 / 配送方式 / 下单时间 / 订单状态），再给「商品与订单的对应关系」。**旧实现里 `const buyer = order?.buyer` 引用了一个不存在的 `order` 变量**（单订单时代的残留），改成多订单后一并修掉。

27. **门店APP 收货管理按批展示**（2026-09-24 elton 定：卡片 = 一批货）：
    - **一张卡 = 一张供货单**，不再是「一笔销售订单一次收货」。数据直接读 `supplyStore`（与后台同一份），确认收货走后台同一个 `applyReceive` —— 两端不会各说各话。登录门店是 **濮源直播间**（`STORE_SELF`）。
    - 卡上字段与后台同口径：供货单号 / 供货路径 / 商品（「X 等 N 种」）/ 规格（单一商品才占一行）/ 应发或发货数量 / 发货主体 / 发货时间 / **物流单号（多个包裹时显示「共 N 个包裹」）**。
    - **每张卡第一个操作仍是「关联订单」**，但点开列的是这一批的**全部**订单（订单号 / 下单人 / 电话 / 商品）—— 一批覆盖多笔自提单，货到要挨个通知客户来取。
    - **物流轨迹按包裹分页签**，与后台同一处理：超过 5 个时前 4 个平铺、其余收进「更多 N 个」下拉，选中第 5 个及以后时下拉本身变成当前包裹的页签。
    - 详情页**实收按商品逐行登记**（店员照着货点数，不按订单点）；少发时**差异原因必选 + 说明必填 + 图片至少 1 张**，字段与后台收货管理、配送差异举证一致。
    - 收货管理的**页签与状态筛选提到了 `StoreApp`**：进详情再返回不该跳回第一个页签（原来 Receipts 重新挂载就重置）。
    - 种子里 **濮源直播间 补齐了三个供货模式 × 五个状态**（新增 `FHD2609190001`–`FHD2609190012`），否则门店端点某个页签是空的。返厂来源 `returnSourcesOf` 也从「本门店已收货的供货单」派生，不再写死演示卡。
28. **门店APP 的状态词**：`stateOf` 把「已发货」「部分收货」都显示为 **待收货**（部分收货另挂标记），只有确认收货那一刻实收 ≠ 应收才判「收货异常」——与后台收货管理同一口径。

## 版本边界（不做）
采购、供应商发货入库、真实库存、真实支付与分账、渠道归因、多语言。

## 架构与技术取舍
- 单一 store（`store.js`）承载运行时单据流转，页面从 store 派生视图，保证「操作 → 状态变化 → 各端同步」在原型里真实发生
- 种子数据集中在 `data.js`，四端共用同一套单据，避免各端演示数据互相矛盾
- 标记用 `.hl` / `data-hl` + 顶栏开关，而不是给研发另出对照表——差异直接长在页面上
- **面向租户的表单文案用租户口吻**（2026-09-23 elton 明确：要「让租户看得懂」）。发货模式这类多档说明**只显示当前选中那一档**，不要三档铺开让人自己找对应；也不要写「租户后台只读可见」「供应商只做签收验收」这种评审口径——那是给内部看的，不是给用的人看的。
- **标签值行的排版：一行只放一个字段**（2026-09-23 elton 要求）。商品与规格不拼在同一行（`商品　规格 × 数量` 这种），拆成「商品 / 规格 / 退货数量」各一行。已铺开到门店APP 返厂列表与详情、配送差异的「来源供货单」、租户/供应商后台返厂详情。**例外**：列表单元格的「名称 + 规格小字」两行式（`<div>{product}</div><small>{spec}</small>`）不算拼接，保持原样。
- **需求注释（`reqnotes.jsx`）的写法与排版**（2026-09-23 elton 要求：简化、不漏细节、编号分行）：
  - 渲染器统一处理，数据里不用手工排版：列表自动编号（`1. 2. 3.`，前后置共用一个连续序号）、行内 `①②③` 自动拆成每条一行、`**加粗**` 渲染成粗体、整条只有「—」的占位与空章节直接不渲染。**别再往数据里写 `·` 或手工换行。**
  - **「口径说明」不与「业务规则」重复**：只在规则没讲到、但评审时会被问到的地方才写（原型简化点、金额/信息脱敏口径、弹窗分几段、页签为什么这么分）。这一轮删掉的 11 处 kj 全是规则的复述。
  - 一条规则只讲一件事：讲四件事就拆成四条，或写成「引导语：①②③④」让渲染器拆行；`③` 里塞两个分支同样要拆开。
  - 口径变更时**必须回查注释**——这一轮修掉 4 处过时口径（都还写着「代发退款由供应商执行」，与「总部管钱、供应商管货」矛盾）。

## 已知坑
- 改过种子数据或默认值后，浏览器必须 `location.reload()`，否则验的是旧 state（HMR 保留 useState）
- `node_modules` 是软链，别跑 `npm install`
- git 提交/推送走 `bash ~/proto-push.sh`（沙箱内不能写 .git）
- 详情/弹窗组件禁止写死数据——曾出现多张卡跳到同一张写死单据
- **列表有的字段，详情也要有**——发货任务列表有「关联销售订单」列，详情抽屉原来没有，对不上账（2026-09-24 补上：详情头部给「任务来源 + 关联销售订单 N 笔」，商品明细给「订单关联」列，与发货弹窗同一套 `OrderRefsPop`）

## 外部资源
- 线上预览：https://eltonliz.github.io/saas-fulfillment-prototype/ （GitHub Pages，从 main 的 `docs/` 部署）
- 对标用的真实 SaaS 测试环境：地址与账号凭据不落盘，需要时向 elton 索取

## 变更记录
- 2026-09-21 建立 AGENTS.md / MEMORY.md；口径以本文件为准
- 2026-09-24 发货 / 收货改为「按商品汇总到门店」的批次模型（订单池 → 生成发货任务 → 待发货 → 发货填物流多包裹）；发货地址只从地址库选且默认唯一；商品明细加「订单关联」；供应商地址库去掉仓库地址；供应商侧买家信息脱敏
- 2026-09-24 门店APP 收货管理按批展示（卡片=供货单，数据与后台同源，确认收货回写后台）
- 2026-09-24 供货单只服务自提单（订单池排除快递单、种子 `from` 换成真实自提单、自提单去掉收件人字段）；页签定名「自提订单 + 发货任务」；发货弹窗地址/订单关联列/供应商脱敏已补
